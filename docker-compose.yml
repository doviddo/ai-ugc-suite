version: '3.9'

services:
  ai-ugc:
    build: .
    container_name: ai-ugc-suite
    restart: unless-stopped
    ports:
      - "5001:5000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./library:/app/library
      - ./assets:/app/assets
