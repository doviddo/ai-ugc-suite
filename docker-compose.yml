version: '3.9'

services:
  ai-clipper:
    build: .
    container_name: ai-clipper
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./library:/app/library
